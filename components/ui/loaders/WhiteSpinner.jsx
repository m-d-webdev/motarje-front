import classNames from 'classnames'
import React from 'react'

const WhiteSpinner = ({className}) => {
    return (
        <div className={`${className} Whitespinner`}></div>
    )
}

export default WhiteSpinner
